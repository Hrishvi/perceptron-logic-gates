Readme:
Train a perceptron using only raw Python
Save trained models with pickle
Load and test saved models
Includes support for:
AND gate
OR gate
NOR gate
Stores trained models in a Trained_Models folder






(Used AI for Grammar)
